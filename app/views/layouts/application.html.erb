<!DOCTYPE html>
<html>
<head>
  <title>Ruby on Rails</title>
  <link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Pacifico">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
    <header>
        <div id="header-wrap">
            <h1><%= link_to "Ruby on Rails", root_path %></h1>
            <% if profissional_autenticado? %>
                <%= render '/layouts/profissional_navbar' %>
            <% else %>
                <%= render '/layouts/visitante_navbar' %>
            <% end %>
        </div>
    </header>

    <div id="content">
        <% if notice.present?  %>
            <p id="notice"><%= notice %></p>
        <% end %>
        <% if alert.present? %>
            <p id="alert"><%= alert %></p>
        <% end %>
        
        <%= yield %>
    </div>
</body>
</html>